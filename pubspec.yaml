name: drone_protection
description: "Drone Aid Security - bouton d'urgence anti-bandit avec geolocalisation en temps reel."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  http: ^1.2.2
  shared_preferences: ^2.3.3
  geolocator: ^13.0.2
  socket_io_client: ^2.0.3+1
  provider: ^6.1.2
  image_picker: ^1.2.3
  record: ^6.2.1
  http_parser: ^4.1.2
  crop_your_image: ^2.0.0
  camera: ^0.12.0+2
  geocoding: ^3.0.0
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
